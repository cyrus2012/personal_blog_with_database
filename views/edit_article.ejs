<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Blog</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB" crossorigin="anonymous">
</head>

<body>
  <div class="container">
    <div class="row justify-content-center">
    <div class="w-75">
      <% if(locals.article){ %>
        <h1>Update Article</h1>
      <% } else { %>
        <h1>New Article</h1>
      <% } %>

      <form action="/modified" method="POST">
        <input type="text" id="title" name="title" placeholder="Article Title" autofocus 
            class="mb-3 w-100" value="<%= locals.article? locals.article.title:''; %>"><br>
        <input type="date" id="date" name="date" placeholder="Publishing Date" 
            class="mb-3" value="<%= locals.date? locals.date:''; %>" %>><br>
        <textarea type="text" id="content" name="content" placeholder="Content" 
            class="mb-3 w-100" rows="20"><%= locals.article? locals.article.content:''; %></textarea><br>
        <% if(locals.article){ %>
        <input type="hidden" id="articleId" name="articleId" value="<%= locals.article.id %>">
        <input type="submit" name="action" value="Update" class="btn btn-primary">
        <% }else{ %>
        <input type="submit" name="action" value="Publish" class="btn btn-primary">
        <% } %>
      </form>
</div>
    </div>
  </div>
</body>

</html>